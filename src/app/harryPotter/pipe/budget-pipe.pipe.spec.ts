import { BudgetPipePipe } from './budget-pipe.pipe';

describe('BudgetPipePipe', () => {
  it('create an instance', () => {
    const pipe = new BudgetPipePipe();
    expect(pipe).toBeTruthy();
  });
});
